# 最终版发布验收

## 20260916.3：质检与采集记录一致性更新

本次上线前完整回归 **267 项通过**；两台副服务器各 **66 项相关测试通过**。主服务器与两台副服务器已部署同一源码版本。质检规则版本为 `zerith_qc_7`。

- 取消升降柱高度检查和旧目录高度命名要求。接受左手、右手及先左后右三种任务模板，商品名称不限；格式错误给出具体原因。
- 人工等级、备注与问题标签持续保留，重复质检和报告升级不会覆盖；默认增量处理新数据，复用符合条件的已有报告及模型结果。
- 等级保存采用单条更新和版本冲突检查，B/F 支持可选原因备注及问题筛选。
- 时间戳、静止、提示词、动作复核或模型连接异常等按现行规则进入预警/待复核；结构损坏、维度或帧对齐错误、非有限数值等仍保留硬失败保护。
- 未选择类别识别时跳过对应视觉检查；动作复核与视觉检查分别说明。
- HDF5 到 LeRobot 转换和左右阶段切分不再要求先通过质检，转换后质检为可选操作。文件可读性与转换本身必需的数据条件仍需满足。
- 通过唯一标识关联采集记录与重命名后的实际目录，保留原始采集编号，支持原始编号和当前目录检索。
- 修复旧格式人工等级选择迁移、自动任务数值检查覆盖人工等级、旧升降柱报告阻止复核及历史时间戳误判 F 的遗漏路径。

浏览器完成回放、等级保存、筛选及窄屏等相关验证；采集系统同期完整回归 **86 项通过**。本轮浏览器验证使用 Chromium，未直接在用户 Edge 客户端操作。软件验证不替代新版本现场实机短录、保存、回放、评级及转换验收；该现场环节尚待完成。CPU 环境可运行质检服务，视频转换速度取决于分配的编码资源。

以下保留初版发布记录。

版本：1.0.0。分支：`dataqc`。仓库中的系统目录：`dataqc/`。

## 验证结果

- 完整回归测试：**179 passed**，覆盖真机／仿真 HDF5、等级优先级、批量模型复核、人工保存、静止处理、LeRobot 转换与切分。仅有依赖库的 TestClient 弃用提示。
- 命令：`PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider`。
- 在新的仓库目录启动 Web 服务，验证模块均从新路径加载，LeRobot 0.3.3 读取组件可导入。
- Chromium 真实数据只读验证：主页面、待复核筛选、三视角回放，在 1920×940、1536×740、1366×680 的浏览器可用区域内正常显示；仿真三路视频正常播放，无页面脚本错误。
- 服务模板通过 `systemd-analyze --user verify`。
- 发布文件检查：无实际 API 密钥、运行数据库、模型权重、采集视频、HDF5、日志或虚拟环境；无大于 10 MB 的文件。
- `pyproject.toml` 固定版本与验收机器已安装版本一致。

## 整理内容

移除开发截图、实验日志、一次性升级脚本、旧采集／ROS 工程副本、重复测试与安装器缓存。
将仍需使用的测试数据生成器提取到当前测试目录，保留实际运行的兼容层与第三方许可证。

把当前使用、部署、数据格式、等级冲突规则集中到 `docs/`，提供不含凭据的配置示例与路径可迁移的服务模板。

本机已有运行目录、人工复核等级、模型结果缓存、导出数据和 Python 环境保留；本地清理前源码备份不随仓库发布。
