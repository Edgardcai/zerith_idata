# 采集网站测试记录

日期：2026-09-08。已完成当前机器人上的双阶段短采集现场验收，8090 用户服务已部署并运行。

## 真机采集

网站直接持有 `MetaTransfer`，提交任务 `8090 / web8090_acceptance`；配置落盘后，由操作员使用 Meta Quest 录制两条数据。

| 数据 | 帧数 | 时长 | 实际频率 | 阶段 | 当前评级 |
|---|---:|---:|---:|---:|---|
| episode_000001 | 630 | 21.039 s | 29.897 Hz | 2/2 | A |
| episode_000002 | 549 | 18.338 s | 29.884 Hz | 2/2 | A |

保存目录：`/data/zerith_data/8090_web8090_acceptance`。两条均保留，未修改历史 picotest 数据。

- 实际收到 EPISODE_START、EPISODE_PROGRESS、EPISODE_END，包含 UUID、阶段与成功结果。等待首个录制事件时，厂商不会发送普通 ready 消息；网站用任务元数据落盘确认接收。
- 开始、阶段、保存、顺序编号、默认 A 和页面刷新后的状态恢复通过。
- HDF5 动作/状态维度、帧数、有限数值、时间戳与三个图像字段检查通过。
- 六个 MP4 全部逐帧解码；每路解码帧数分别为 630、549，与对应 HDF5 一致。
- 第二条录制期间打开、关闭三路 640×480 预览，录制继续完成。关闭预览时厂商记录一条 WebRTC ICE 清理告警，现归入预览诊断，不再误报为数据写入告警；原始日志保留。
- 浏览器内实际操作第一条的 B → F → A、查看三路已保存视频，均成功；视频元数据加载无错误。
- 保存后通过网页“结束会话”取消网站的真实 gRPC 调用，网页确认连接关闭。此项只验证释放采集连接，不把它当作物理急停或厂商任务清除指令。
- 操作员随后反初始化，前检正确拒绝新任务。没有为了后续测试自动初始化机器人。

## 自动化与浏览器

24 项 unittest 全部通过，覆盖配置、路径、VR 新鲜度、gRPC 序列化/取消、模拟传输中断不重提任务、重复启动和录制中结束拒绝、编号/默认 A/评级/删除、未完成及异常数据保护、改名/评级/日志偏移恢复、HTTP 来源和令牌校验、同步启动失败可重试、预览告警分类。

`tests/browser_smoke.py` 使用独立临时服务器和临时数据，实际点击 A/B/F、放弃确认，检查文件确实删除及刷新，390 px 宽度无横向溢出，JavaScript 页面错误为零。真实机器人页面也完成 1440 px、390 px 截图和相机开关测试。

删除测试使用临时数据，真实两条验收数据均未删除。网络异常测试使用模拟 gRPC；没有断开机器人网络或停止厂商服务进行故障注入。

## 部署与边界

- 独立目录 `/home/robot/collection_web`，独立 8090 服务；不导入控制项目，不调用 8080 API。
- 五个关节通过只读 ZCM 订阅更新；相机通过独立 SDK 客户端接入，默认关闭。
- 服务重启后，两条索引、编号、评级正确保留，没有重新提交采集任务。
- 未通过停止 8080 进程来做独立性验收；独立性已核对代码依赖及直接设备接口。
- 厂商服务启动、VR 佩戴连接和机器人初始化沿用现有流程。网站完成软件前检及采集接口调用，不自动重启 robotd 或发送运动指令。
- A/B/F 写入独立索引及 `review.json`，不会自动改变厂商训练转换器的筛选行为。
- 本次为两条短采集验收，不代表已做整日连续采集或所有硬件故障测试。

## 本机证据

- `runtime/live_acceptance.jsonl`：采集期间状态快照。
- `runtime/meta_2714c014964a4e2f92bde2a8217d1196.jsonl`：厂商真实事件。
- `runtime/video_decode_acceptance.json`：六路视频逐帧解码结果。
- `runtime/acceptance_desktop.png`、`acceptance_cameras.png`、`acceptance_mobile.png`、`acceptance_video_review.png`：浏览器现场截图。

复测命令：

```bash
cd /home/robot/collection_web
.venv/bin/python -m unittest discover -s tests -v
.venv/bin/python tests/browser_smoke.py
node --check static/app.js
```

## 商品与高度目录更新验收

2026-09-08：31 项自动化测试通过，新增验证目录名规范化、厂商请求内容保持一致、只有完成后才归档、原目录历史数据保护、同目录继续编号、移动失败保留原文件、移动后重启恢复、不同高度分目录、归档后的 HTTP 视频/评级/删除。浏览器实测左右商品与高度输入实时生成正确目录，390 px 无横向溢出，无 JavaScript 错误。

已在操作员允许结束会话后更新 8090 服务，升级前备份 SQLite，新旧会话索引兼容。归档流程使用临时文件和模拟厂商 gRPC 验证；本次更新没有重新启动真机录制。

## 1.0 遥操提示与固定升降柱（2026-09-08 19:12）

网站 34 项测试、浏览器交互及 LAN 实际打开通过。配套遥操扩展已部署，标定成功可视提示、可选浏览器语音、高度配置接口和偏差预警已接入。配置仅反初始化且无正在录制数据时保存，下次初始化读取；原有目录高度标签保持独立。

真机新采集 `episode_000003`：999 帧、33.38 秒、29.90 Hz；头腰四轴 action 为 0，最大实际角度偏差 0.01431 rad；升降柱 0.4 m 最大误差约 0.000033 m。X 锁定/解锁期间保持，未发生角度预警导致的暂停。无重复/倒退时间戳，但有 3 个约 66.7 ms 的间隔。详见配套 `teleop_zero_lock/TEST_REPORT.md`。
