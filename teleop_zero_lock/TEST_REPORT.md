# 头腰锁零试验记录

2026-09-08，本机 ZERITH-H1 1.3.9。

**最新结论（19:12）：1.0 已部署，偏差仅预警、网页标定提示、升降柱固定 0.4 m 已完成本机短采集验证。早期失败与未完成事项保留为历史记录，最终范围见文末。**

## 离线验证

- 17 项 Python 测试通过：平滑头部设定速度、真实反馈过期/错误、连续稳定检查、未就绪 A 键拦截、错误/反初始化不发送头部动作、暂停捕获当前双臂位置、末端输出防覆盖、切换前置检查。
- 独立可执行文件内 14 项策略测试通过；自检入口先封禁真实 ZCM 构造。
- 真实 URDF 的 40 组 FK 比对（原模型将四轴设零，对比受约束模型），最大数值差 0；四个固定轴的 FK 导数为 0。
- 真实厂商 IK 求解 3 轮输出有限，四轴投影为零；重标定改变人体比例后约束保留。保留原有碰撞、限位成本。
- 通过内存总线执行真实厂商初始化、普通下发、另一条 motor-control 下发、头部下发、反初始化；验证四轴输出约束、双臂目标及头部增益不变，反初始化命令保持厂商语义。
- 控制器内部的反馈 WBC 保持原模型；非零实际头腰姿态对应的末端 FK 与原厂一致。
- 厂商启动时还加载 16 轴 HIGH_LEVEL 模型；该模型已排除在 VR 约束之外并通过构造兼容测试。
- PyInstaller 重建仅替换 teleop 入口，其余 643 项逐项校验原始归档字节相同。原始二进制 SHA 固定校验；自检结果绑定当前试验二进制 SHA。
- 日志及 JAX 缓存使用试验目录，未修改厂商配置文件。

## 真机进展

18:09 首次切换：原 teleop 正常退出；试验构造器发现 HIGH_LEVEL 的 16 轴模型也被约束，主动拒绝启动。此时机器人反初始化，未发起初始化或归零。已将此问题修正并增加离线回归测试。

18:11 重新启动修正版；后续初始化和真实运动验证见下文。

用户明确要求不额外设置电量门槛，已移除该自设门槛；保留原厂保护及电机、通信、VR 状态检查。

## 当时尚未完成的验收（早期历史记录）

- 按用户接受的头 pitch 约 ±0.02 rad 标准进行更长时间及实际任务验证。
- 不经过反初始化/重标定的连续暂停恢复、头部摇杆的专项验证。
- 急停的真机行为（保留原厂处理，但本次未触发实测）。
- 正式短 episode 中四轴 action/state、双臂定位误差及采集帧率。

这些完成前，不将试验版标为日常采集已验收版本。观测记录在 runtime/trial_*.jsonl，判定以实际反馈为准。

## 真机第二轮与用户接受标准

- 初始化后，总线实测 head_control 角度 [0,0]、KP/KD [6,6]/[1,1]；原厂无前馈时头 pitch 稳定在约 0.073815 rad，头电机保持力矩约 −0.46 N·m。
- 原厂实际控制程序中的 head_range 包含零点；URDF 头 pitch 范围 −0.7854～0.5236 rad。离线模型头 pitch 重力扭矩约 −0.384 N·m（零位）、−0.409 N·m（实测初始角度）。
- 加入缓慢重力前馈及上限 ±0.15 N·m 的积分修正后，头 pitch 降至约 0.020027 rad，连续 20 秒无明显漂移；此时前馈约 −0.541 N·m，头 yaw 约 −0.000191 rad，腰两轴约零。
- 用户明确接受约 0.02 rad 的小偏差。因此没有部署随后准备的更大补偿范围版本，保留已测补偿设置；头 pitch 就绪容差改为 0.021 rad（含采样余量），其他三个角仍为 0.005 rad。
- 后续 A 启动及实际运动结果见下文；实际录制仍待验证。

## 第四版真实运动验证

18:30 首次进入遥操发现补丁 HeadCtrl 返回类型不符合厂商接口（厂商需要字典），已修正。第四版自检增加真实厂商过渡首帧、过渡结束和连续遥操处理函数的接口验证；本轮运行版本 SHA256 为 `0a91cab6ce9b0a79dd4c7482d7d0c9ff8ecfefaf0551563998b92182013e8473`。

18:37:01 进入遥操，18:37:04 完成过渡；18:39:10/16 发生 X 锁定/解锁，采样中的头腰四轴角度目标始终为零。18:39:32 用户反初始化，随后重新初始化、标定。期间出现手柄位姿丢失，厂商程序暂停并要求重新标定。

18:45:55.098 至 18:46:40.010，第二次连续运动约 44.9 秒。18:45:57.609 完成平滑过渡，双臂 14 个关节均有明显实际运动，单关节运动范围约 0.64～2.17 rad。记录到的运行频率约 73～93 Hz；这不是数据集采集帧率。

| 关节 | 实测最小值 rad | 实测最大值 rad | 最大绝对偏差 rad |
| --- | ---: | ---: | ---: |
| 腰 pitch | -0.000407464 | 0.000431432 | 0.000431432 |
| 腰 yaw | -0.000167779 | 0.000119842 | 0.000167779 |
| 头 yaw | 0.000190735 | 0.000190735 | 0.000190735 |
| 头 pitch | -0.010108948 | 0.015830994 | 0.015830994 |

来源：`runtime/teleop_joint_trace.jsonl`，各状态通道 179 个样本，各指令通道 176 个样本，约 4 Hz 只读采样。头腰四轴所有采到的角度目标均为 0；升降柱实际在约 0.143～0.520 m 间运动，仍沿用厂商遥操逻辑。汇总存于 `runtime/motion_acceptance_184555.json`。采样无法排除间隔内瞬时峰值，双臂末端精度也未单独验收。

18:46:39.999 用户短按 A，成功暂停。18:46:46.889 左手柄数据丢失，厂商原有保护重置规划并要求重新标定。此轮没有验证直接暂停后恢复，也没有启动采集会话（监测中 phase 始终 idle），不能据此声称 HDF5 录制已验收。

另发现只读监测器使用 HTTP 请求前时间检查请求后读取的状态文件，可能把正常的新状态误标为 stale；本轮 166 条网站采样中有 17 条被该判定标记。已将监测器比较时间改为文件读取后的当前时间，历史记录保留原样。上述角度结果来自独立总线记录，不依赖此就绪判定。

## HDF5 录制验收（18:59 更新）

测试目录 `/data/zerith_data/8099_head_waist_zero_lock_acceptance`，网站直接持有单阶段、30 Hz 的 MetaTransfer 会话。两条均已完成保存并按顺序编号。

- `episode_000001`（18:49:32～18:50:52）：2398 帧、80.086 秒、29.930 Hz。期间发生暂停、反初始化及重新初始化，头 pitch 最大 0.051309 rad，1265 帧超过 0.021 rad，不符合锁零采集要求。网站自动默认 A 不等同于锁零验收通过；原始数据及评级未改动。
- `episode_000002`（18:58:26～18:58:46）：598 帧、19.972 秒、29.892 Hz。HDF5 四轴 action 全部严格为 0；四轴 state 最大绝对值依次为腰 pitch 0.000371511、腰 yaw 0.000479369、头 yaw 0.005149841、头 pitch 0.007057190 rad。全部小于用户接受的约 0.02 rad，但头 yaw 有 1 帧超过试验设定 0.005 rad，18:58:39.987 自动暂停。因此角度达到用户期望，连续操作体验仍未验收通过。
- 第二条所有数值字段有限且长度一致；三路相机 color/depth 长度均为 598，各取首/中/尾编码样本非空（不等同于完整视频内容验收）。有 1 个重复时间戳、无倒退，3 个间隔超过 50 ms，最大约 66.704 ms。
- 用户提出持续跟踪，本轮从初始化、标定、归零、遥操开始、Y 录制、自动暂停到保存完成，持续监测并主动通知，没有依赖额外聊天回复驱动检查。

只读分析脚本 `tools/analyze_recording.py`，报告 `runtime/episode_000002_audit.json`；正式采集文件均未修改。当前启动/运行共用就绪判定，单帧轻微超限也会暂停，这是下一步需修正并验证的操作问题，不能将本版本视为完整日常采集交付。

## 完整版 1.0 最终验证（19:12）

二进制 SHA256：`c567f604ef889ed0d3afbfe1eb993b0aa373b84cc8282741939d0b7a196cb7cd`。19:08:35 完成启动，当前启用 `lift_enabled=true / lift_height_m=0.4`。

### 软件验证

- 遥操主机测试 19 项通过，包含偏差/归零等待不暂停、反馈故障仍暂停、高度参数范围、速度/位置投影与原有补偿限制。
- 二进制内 16 项策略测试及真实厂商模型/接口测试通过；包含头腰 40 组 FK、升降柱 0/0.4/0.8 m 的 24 组 FK 与五个固定自由度的零 Jacobian。
- 真实厂商内存总线验证：平滑初始化到 0.6 m、正常与 motor-control 路径高度保持、X 缓存/高度偏移不能覆盖、重复标定不累加高度、关闭固定后恢复原厂 0.4 m 初始化配置。
- 采集网站 34 项测试通过；浏览器测试通过标定成功显示、偏差预警不覆盖“遥操作中”、运行时高度设置禁用、手机无溢出、原有评级/删除/目录预览。
- 本机通过 LAN 地址 `http://172.16.18.43:8090` 打开新版，浏览器无脚本错误。语音提示由浏览器 Web Speech API 提供；可视提示已验，实际播音取决于浏览器语音及用户音频设备。

### 真机验证

- 19:10 初始化，升降柱实测平滑到 0.4 m；19:10:37 标定成功状态与序号正常更新，19:10:40 进入遥操。
- 19:10:45 头 pitch 0.02117 rad 时仅预警，状态仍为 DECOUPLED，未暂停；随后自行稳定。19:11:00 用户短按 A 暂停，19:11:35 短按 A 恢复成功。
- `episode_000003` 19:11:41.356714～19:12:14.732509：999 帧，33.375795 秒，29.901909 Hz。全帧头腰四轴 action 严格为 0。
- 腰 pitch / 腰 yaw / 头 yaw / 头 pitch 最大绝对值分别为 **0.001090564 / 0.000551274 / 0.004386902 / 0.014305115 rad**；均满足此前检查范围，也均小于用户接受的 0.02 rad。
- 升降柱 action 全程为 float32 表示的 0.4 m（0.40000000596）；state 范围 **0.399976075～0.400033087 m**，最大绝对偏差 **0.000033087 m**。
- 14 个双臂关节均有明显运动，单关节运动范围约 0.58～1.85 rad。19:11:57 / 19:12:06 的 X 锁定/解锁没有改变升降柱或头腰目标。录制期间无自动暂停。
- 数值字段有限且全帧长度匹配，相机字段长度匹配、首中尾编码样本非空。时间戳无重复或倒退；有 3 个间隔大于 50 ms，最大 66.716 ms，不能声称逐帧严格等间隔 30 Hz。
- 分析结果：`runtime/episode_000003_audit.json`，完整只读总线记录 `runtime/complete_version_joint_trace.jsonl`。

### 验收边界

本轮实际测试的固定高度为 **0.4 m**，其他高度做过模型及控制接口离线测试，未逐个真机验证。没有触发物理急停；原厂保护未修改。三路图像未逐帧人工审阅，双臂绝对末端精度和长时间运行未专项测量。未修改开机启动，重启原厂服务后需按 README 重新启用扩展。网站默认 A 是可修改的人工评级，不等同于自动通过本报告的运动学验收。


## 2026-09-09 启动检查修复

外层跳过保存检查未同步到嵌入二进制，导致切换后扩展退出；已同步传参并重建。增加已停止遥操的显式恢复入口及基于进程 PID、新鲜状态的启动确认。关闭的采集会话现在可正常通过检查。

24 项主机测试通过，含嵌入启动检查参数、启动成功/失败回归。候选二进制真实厂商离线自检通过（16 项策略测试及模型/控制接口验证），SHA256：`33e79508de701975f0b8faafb756e32f8c6401ff3eb0d00f6b52dbb3d5ea64be`。日志：`runtime/self_test_startup_fix.log`。旧版本及清单备份：`runtime/backups/startup_fix_20260909`。头腰/升降柱控制补丁未变。本次尚未启动真机版本，需操作员通过 sudo 执行恢复命令。
